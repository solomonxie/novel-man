import { AppRegistry } from 'react-native';

import App from './src/navigation/Navigator';

// The name the native side starts: see `startReactNative(withModuleName:)`.
AppRegistry.registerComponent('main', () => App);
